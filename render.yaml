services:
  - type: web
    name: pdf-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PUPPETEER_SKIP_DOWNLOAD
        value: false
    runtime: node
    build:
      nodeVersion: 20
